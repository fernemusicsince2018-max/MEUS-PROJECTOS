[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions."notifications-dispatch-scheduled"]
  schedule = "* * * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
